import { makeStyles } from '@material-ui/core/styles';

export default makeStyles({
    component: {
        marginTop: 40,
    },
    logout: {
        fontSize: 14,
        marginLeft: 20,
        padding: 0
    }
})